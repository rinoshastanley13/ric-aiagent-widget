version: '3.8'

services:
  widget:
    build: .
    container_name: ricagent-widget
    restart: unless-stopped
    ports:
      - "3001:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - NEXT_PUBLIC_CHAT_API_URL=http://106.51.109.172:5500
    env_file:
      - .env
